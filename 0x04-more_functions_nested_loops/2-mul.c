#include "main.h"

/**
 * mul - Mulitiplcation two integars 
 * @a: The first number to multiply 
 * @b: The second number to multiply with first number 
 * Return: a and b 
 */

int mul(int a, int b)
{
	return (x * y);
}
