#ifndef FUNCTION_LIKE_MACRO_H_
#define FUNCTION_LIKE_MACRO_H_

/**
 * main - compute absolute value
 *
 * @x: input value
 *
 * Desc: header file that compute abslute value
 */

#define ABS(x) ((x) < 0 ? (-1) * (x) : (x))

#endif
