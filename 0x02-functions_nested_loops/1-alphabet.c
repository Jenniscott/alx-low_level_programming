#include <stdio.h>
#include <stdlib.h>
/**
 * print_alphabet - Entry point
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char n = 'a';

	while (n <= 'z')
	{

		putchar(n);
		c++;
	}
		putchar("\n");
		return (0);
}

